# Database Configuration
# Untuk serverless (Vercel), gunakan connection pooler URL (PgBouncer/Supabase Pooler/Neon Pooler)
# Tambahkan ?pgbouncer=true&connection_limit=1 di akhir URL untuk serverless
DATABASE_URL="postgresql://user:password@host:6543/dbname?pgbouncer=true&connection_limit=1"
# Direct URL tanpa pooler — untuk migrations (prisma migrate)
DIRECT_URL="postgresql://user:password@host:5432/dbname"
JWT_SECRET=

# Server Configuration
PORT=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Callback must match the one configured in Google Cloud Console
# For local dev, use: http://localhost:3000/v1/auth/google/callback
GOOGLE_CALLBACK_URL=/v1/auth/google/callback
